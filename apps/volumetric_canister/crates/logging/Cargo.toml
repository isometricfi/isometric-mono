[package]
name = "logging"
version = "0.1.0"
edition = "2021"

[dependencies]
canlog = { version = "0.2.0", features = ["derive"] }
ic-http-types = "0.1.0"
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
sha2 = "0.11.0"

[features]
debug = []
