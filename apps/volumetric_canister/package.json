{
  "name": "@volumetric/canister",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cargo build --release --target wasm32-unknown-unknown -p volumetric_canister_backend",
    "build:dfx": "dfx build",
    "dev": "dfx start --background && dfx deploy",
    "test": "cargo test -p volumetric_canister_backend"
  }
}