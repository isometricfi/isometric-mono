meta {
  name: Get Events Since
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/trpc/events.getSince?input={"json":{"timestampMs":{{timestampMs}}}}
  body: none
  auth: none
}

vars:pre-request {
  timestampMs: {{$timestamp}}
}

