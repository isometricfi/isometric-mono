meta {
  name: Sync Balance
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/trpc/account.syncBalance
  body: json
  auth: none
}

body:json {
  {
    "json": {
      "address": "{{address}}"
    }
  }
}
