meta {
  name: Create Account
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/account/create
  body: json
  auth: none
}

body:json {
  {
    "address": "{{address}}",
    "signature": "{{signature}}"
  }
}
