meta {
  name: Create Account
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/trpc/account.createAccount
  body: json
  auth: none
}

body:json {
  {
    "json": {
      "address": "{{address}}",
      "signature": "{{signature}}"
    }
  }
}
