meta {
  name: Test Expire Option
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/options/test-expire
  body: json
  auth: none
}

docs {
  Testing endpoint - expires an option immediately for testing settlement
}

body:json {
  {
    "optionId": "1"
  }
}
